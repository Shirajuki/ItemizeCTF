name: robots
author: Shirajuki
description: |-
  This website has quotes from the robot Bender. 
  I <3 robots
  {{ link }}
flag: flag{r0bots_4re_superduPer_fun} 

containers:
  main:
    build: .
    volumes:
        - '.:/app'
    ports:
        - [5000]
    restart: always
    stop_signal: SIGKILL  

expose:
  main:
    - target: 53311
      http: robots
